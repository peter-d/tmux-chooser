* Docs
  * Add screenshot to README

* Config file: more flexible: different user per server?
* Tests?

* Rewrite the async session checking
  * Maybe use something like twisted or fancy new python3 features?
